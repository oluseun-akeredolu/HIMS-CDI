import sys, logging; sys.path.insert(0,r"phase-1-cic-iomt/src"); sys.path.insert(0,r"phase-1-cic-iomt/scripts"); logging.basicConfig(level=logging.DEBUG, format="%(levelname)s: %(message)s"); from csra.streaming_engine import initialize, process_event; initialize(); r=process_event({"event_id":"test-bad"}); print("RESULT:", r); print("PASS - BUG FIXED" if r is None else "FAIL - BUG STILL EXISTS")